(function () {
    'use strict';

    /* Filters */
    angular.module('ccportal.products.otp.filters', []);

    var OtpFilters = angular.module('ccportal.products.otp.filters');

    // OtpFilters.filter('MessagingGwEDRTypeFilter', function (MSGGW_EDR_TYPE) {
    //     return function (typeKey) {
    //         var type = _.find(MSGGW_EDR_TYPE, function (edrType) {
    //             return (edrType.type_key === Number(typeKey));
    //         });

    //         if (type)
    //             return type.text + ' [' + typeKey + ']';
    //         else
    //             return typeKey;
    //     };
    // });


})();
