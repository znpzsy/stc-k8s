apiVersion: v1
kind: Service
metadata:
  name: consportals-sa-mobily-dsp-httpd-service
spec:
  type: ClusterIP
  selector:
    component: dsp-httpd
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 443
      targetPort: 443
